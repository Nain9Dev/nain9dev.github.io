# Honest Offer Copy Plan

1. Tests first:
   - Extend `BLOCKED_CLAIMS` in `scripts/check-claims.mjs` with `99.9x%`
     percentages, p95 latency, uptime readings and `[REDACTED]`, and add matcher
     cases, including negatives for "alta disponibilidad" and "zero downtime"
     used as a technique, to `scripts/claims.test.mjs`.
   - Add `scripts/offer-copy.test.mjs` for COPY-001 to COPY-005: source scan for
     free-audit CTA wording and "disponibilidad inmediata", CTA labels and
     Calendly URLs on the three known pages, hero sentence, About role, terminal
     data (no confidential/NDA project, redacted values, telemetry or figures;
     required commands present; help lists only existing commands; contact
     address), and service wording without "Zero Downtime" benefit labels.
   - Wire the new test into `npm run check`. Observe failure.
2. Apply the copy changes listed in `spec.md`.
3. Run `npm run translations:prepare`, add reviewed American English entries for
   every pending segment (placeholders preserved), and remove catalog entries no
   longer referenced by `.astro/translations/source.json`. New keys are appended.
4. Resolve OQ-008 and OQ-009; update requirements, traceability, tasks and
   changelog.
5. Run the full `npm run check`.

The English terminal file is generated by `scripts/build-locales.mjs` from the
Spanish HTML fragments and the catalog, so terminal segments go through the same
catalog flow as pages.
