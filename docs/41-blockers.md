# Blockers

Every external dependency, listed before writing code. If an open blocker stops progress, the agent stops and says so.

Verification column states what was actually checked on 2026-09-16 and by which means. No credential values are stored here.

| ID | Label | What | Where to get it | Stored in | Unblocks | Verified ready when | Current state |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| B-001 | `[H]` | GitHub Pages hosting with environment `github-pages` restricted to `main` | Repository Settings > Pages / Environments | GitHub repository settings | Deployment, DES-006 | `deploy.yml` run on `main` succeeds and the environment lists `main` only | Verified: successful deploy runs recorded in specs 002 and 003; environment restriction recorded in spec 004 handoff |
| B-002 | `[H]` | Custom domain `www.naindev.com` | Domain registrar and DNS provider | `CNAME` file; DNS records outside the repo | Public URL, canonical URLs (`site` in `astro.config.mjs`) | `CNAME` matches `site`; public HTTPS requests return 200 | Verified in repo: `CNAME` = `www.naindev.com`. DNS records and registrar not inspectable from the repo |
| B-003 | `[H]` | Cloudflare zone ID and API token for cache purge | Cloudflare dashboard (zone overview; API Tokens with Cache Purge permission) | GitHub secrets `CLOUDFLARE_ZONE`, `CLOUDFLARE_API_TOKEN` | "Purge Cloudflare Cache" step in `deploy.yml` | `gh secret list` shows both names and a deploy log shows a successful purge response | **Unverified**: `gh secret list` and `gh secret list --env github-pages` both returned no secrets on 2026-09-16. The `curl` step has no `--fail`, so it likely exits 0 even when the API rejects the request (assumption: purge silently does nothing) |
| B-004 | `[H]` | Cloudflare proxy in front of GitHub Pages | Cloudflare dashboard | Outside the repo | Edge caching, email obfuscation handled by `check-production.mjs` | Production responses show Cloudflare transforms | Partially verified: production verification in spec 003 had to account for Cloudflare email obfuscation. Proxy, TLS and header rules not inspectable from the repo; whether `public/_headers` is applied is unverified |
| B-005 | `[H]` | Plausible analytics site | Plausible account | Script tag in `src/layouts/BaseLayout.astro` (`data-domain="naindev.com"`); CSP allowance in `src/components/SEO.astro` and `public/_headers`; helpers in `src/utils/analytics.js` | Analytics events | Plausible dashboard for `naindev.com` receives page views | Unverified: only localhost-ignore warnings were observed in browser checks (spec 002) |
| B-006 | `[H]` | Calendly 30-minute booking link | Calendly account | Hard-coded URL in `Header.astro`, `Hero.astro`, `ContactSection.astro` and several blog posts | Primary call to action | Link opens a bookable page | Unverified: link presence confirmed in source; availability not checked |
| B-007 | `[H]` | MailerLite newsletter form | MailerLite account | Footer form in `src/components/Footer.astro`; loader `public/assets/js/lazy-mailerlite.js`; checklist form in `src/pages/recursos/checklist-ia.astro` redirects to `/recursos/gracias/` | Newsletter and checklist lead magnet | Form posts to a real account and a test subscription is delivered | **Not ready**: the form uses placeholder identifiers (`xxx`); recorded as a known limit in specs 002 and 003 |
| B-008 | `[H]` | FormSubmit endpoint for lead-magnet forms | FormSubmit | Form actions in some `src/content/blog/*.mdx` posts | Lead capture in those posts | A test submission reaches the owner inbox | Unverified: endpoint presence confirmed in source only |
| B-009 | `[H]` | Google Search Console property | Google Search Console | Outside the repo | T-001 (recrawl request), SEO monitoring | Property shows the home URL as inspectable | Verified in spec 001 handoff (indexing requested); spec 002 recrawl request still open |
| B-010 | `[M]` | Designer access checks for `design/**` ruleset | Designer's GitHub account `davidesuarez` | Ruleset `protect-non-design-branches` (id 23551316) | DES-001, DES-002 closure (T-002) | Four checks in `specs/004-design-collaboration/plan.md` recorded | **Open**: ruleset active; pushes from the designer's account not yet performed |
| B-011 | `[A]` | Optional local translation tooling: NainWrite checkout, Python, Ollama on loopback | Local machine | Session variables `NAINWRITE_ROOT`, `NAINWRITE_PYTHON` | New English drafts (`npm run translations:watch`) | `npm run translations:generate` completes | Not required for builds or CI (LOC-006). Verified working in spec 003 handoff |
| B-012 | `[A]` | Node.js 24 and npm dependencies | nodejs.org, npm registry | `package.json`, `package-lock.json` | Every build and check | `npm ci && npm run check` passes | Verified in CI for released commits (specs 002, 003) |
| B-013 | `[H]` | Public contact email `contact@naindev.com` | Owner decision 2026-09-16 (spec 008); Cloudflare Email Routing on `naindev.com` | Site sources (privacy policy, checklist fallback, terminal) and design brief | MAIL-001, MAIL-002, DES-017 | A Cloudflare routing rule forwards the address and a test message is received | **Partially verified**: MX records point to Cloudflare Email Routing; routing rule and delivery not verified |
| B-014 | `[H]` | WhatsApp Business number for a chat button | Owner (number provided 2026-09-16) | Outside the repository until the chat button is implemented; then a public `wa.me` link in site sources | Contact button without a visible number (DES-017) | A test `wa.me` link opens a chat with the business account | **Partially ready**: number provided, kept out of the repository; link not tested |
| B-015 | `[H]` | Trademark filing for the NainDev logo | OEPM (Oficina Española de Patentes y Marcas) e-filing; similarity search in OEPM Localizador and TMview | Outside the repository | Registered brand identity (DES-020) | Written assignment of the logo rights from the designer, clean similarity search, application filed | **Open**: logo redesign not delivered. The designer agreed verbally on 2026-09-16 that the owner may register it; a written assignment is still required before filing, otherwise the current logo is kept |
